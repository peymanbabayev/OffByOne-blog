services:
  app:
    build: .
    env_file: .env
    expose:
      - "3000"
    networks:
      - shared-web
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
    networks:
      - shared-web
    depends_on:
      - app
    restart: unless-stopped

networks:
  shared-web:
    external: true
