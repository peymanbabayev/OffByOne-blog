server {
    listen 80;
    server_name offbyoneblog.duckdns.org;

    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name offbyoneblog.duckdns.org;

    ssl_certificate /etc/letsencrypt/live/offbyoneblog.duckdns.org/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/offbyoneblog.duckdns.org/privkey.pem;

    location / {
        proxy_pass http://app:3000;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

server {
    listen 80;
    server_name offbyoneblog-dev.duckdns.org;

    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name offbyoneblog-dev.duckdns.org;

    ssl_certificate /etc/letsencrypt/live/offbyoneblog-dev.duckdns.org/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/offbyoneblog-dev.duckdns.org/privkey.pem;

    location / {
        proxy_pass http://app-staging:3000;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
